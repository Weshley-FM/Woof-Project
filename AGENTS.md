# Project Instructions

For all UI work in this repository, use [docs/design-system.md](docs/design-system.md) as the source of truth for colors, typography, buttons, text fields, spacing, and component states.

Before adding new visual styles, check the design guide and extend it only when the user provides an updated reference.
